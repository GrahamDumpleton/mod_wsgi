# Per-test Apache configuration for tests/wsgi/input.
#
# Enable chunked request body support for this test's endpoints
# so we can exercise wsgi.input reading with
# Transfer-Encoding: chunked requests alongside the normal
# Content-Length requests.

<Location /test/wsgi/input>
    WSGIChunkedRequest On
</Location>
